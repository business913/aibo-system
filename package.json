{
  "name": "aibo-webhook-server",
  "version": "1.0.0",
  "description": "AIBO AI不動産管理システム — LINE Webhookサーバー",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "gcloud run deploy aibo-webhook --source . --region asia-northeast1 --platform managed --allow-unauthenticated"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.19.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
